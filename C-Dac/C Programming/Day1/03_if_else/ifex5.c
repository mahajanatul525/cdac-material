#include<stdio.h>
void main()
{
int mks;
printf("Enter a mks");
scanf("%d",&mks); 55  80 30
	if(mks>=70)
		printf("A");

	else  if(mks>=55)
		printf("B");

	else  if(mks>=35)
		printf("C");
	 else
		printf("fail");
}